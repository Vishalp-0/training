function sortbyinitial (names) {
    names.sort();
    let output = {};

    for (let i = 0; i< names.length; i++) {
        let firstletter = names[i][0];
        
        console.log(sortbyinitial());
    }

}